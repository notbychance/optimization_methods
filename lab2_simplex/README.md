# ЛР2. Симплекс-метод

Модуль решает задачи линейного программирования собственным двухфазным симплекс-методом.

## Возможности

- задачи на максимум и минимум;
- ограничения `<=`, `>=`, `=`;
- автоматическое добавление остаточных, избыточных и искусственных переменных;
- фаза I для поиска допустимого опорного решения;
- фаза II для оптимизации исходной целевой функции;
- точные вычисления через `fractions.Fraction`;
- Markdown-снимки всех ключевых таблиц;
- статусы `optimal`, `infeasible`, `unbounded`.

## Структура

```text
lab2_simplex/
├── main.py
├── problem_reader.py
├── simplex.py
├── snapshot.py
├── README.md
└── examples/
    └── lab2_example.json
```

## Запуск

Из корня проекта:

```bash
python main.py -n 2 -i lab2_example.json -o results/lab2.md
```

Из каталога модуля:

```bash
python main.py -i examples/lab2_example.json -s simplex_snapshot.md
```

## Формат JSON

```json
{
  "objective": {
    "type": "min",
    "coefficients": [830, 835, 360, 450]
  },
  "variable_names": ["D1", "D2", "D3", "D4"],
  "constraints": [
    {
      "name": "constraint_1",
      "coefficients": [10, 18, 20, 15],
      "sign": ">=",
      "rhs": 800
    }
  ]
}
```

Числа можно задавать целыми, десятичными или строками-дробями, например `"3/5"`.

## Вариант 17

Файл `lab2_example.json` буквально отражает условие варианта 17. При этих данных задача несовместна: требование по количеству четырехкомнатных квартир слишком велико относительно бюджета 40 млн руб.

Контрольный результат программы:

```text
Статус: infeasible
Сообщение: Система ограничений несовместна: максимум вспомогательной функции W не равен 0.
```

Это корректный математический вывод для исходных чисел варианта.
